%h2= t('forem.admin.group.members', :group => @group.name)

= text_field_tag "new_member"
= button_tag t(".add_member"), :disabled => true, :id => "add_member", "data-group-id" => "#{@group.id}"

%ul#members
  - @group.members.each do |member|
    %li= member.to_s

